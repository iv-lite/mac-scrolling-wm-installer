#!/bin/bash
# JankyBorders config — active-window focus border.
# Loaded by the borders service from ~/.config/borders/bordersrc

options=(
	style=round
	width=4.0
	hidpi=on
	active_color=0x9e2b303c
	inactive_color=0xd62b303c
)

borders "${options[@]}"
